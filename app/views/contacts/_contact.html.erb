<tr><td><%= contact.id %></td><td><%= contact.name %></td><td><%= contact.phone %></td><td><%= contact.notice %></td>
<% if is_in_edit_mode(contact.customer) %>
<td><%= link_to "Destroy", [contact.customer, contact], :confirm => "Are you sure?", :method => :delete, :class => "btn" %></td>
<%end%></tr>
